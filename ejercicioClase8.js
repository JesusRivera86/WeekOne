//Crea una función que imprima por consola el resultado de elevar al cubo
// un número dado como parámetro.

const cube = (number1) => {
  return number1 * number1 * number1;
};
console.log(cube(3));
