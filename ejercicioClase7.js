//Crea una función que, al ser llamada, imprima por consola el resultado de la
//multiplicación de dos números introducidos como parámetros.

const multiply = (number1, number2) => {
  return number1 * number2;
};
console.log(multiply(5, 5));
